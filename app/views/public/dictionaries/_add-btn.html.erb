<% if current_user.dictionary.words.include?(word) %>
  <%= link_to "Remove", remove_dictionary_path(word), method: :delete, remote: true, class: "btn btn-danger" %>
<% else %>
  <%= link_to "Add", add_dictionary_path(word), method: :post, remote: true, class: "btn btn-primary" %>
<% end %>
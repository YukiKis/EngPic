<% if current_user != user %>
  <% if current_user.following?(user) %>
      <%= link_to "Unfollow", users_unfollow_path(user), method: :delete, remote: true, class: "btn btn-danger" %>
    <% else %>
      <%= link_to "Follow", users_follow_path(user), method: :post, remote: true, class: "btn btn-primary" %>
  <% end %>
<% end %>
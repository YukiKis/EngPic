<div class="row">
  <div class="col-sm-12">
    <div class="text-left">
      <% if word_count.present? && tag.present? %>
        <h3 class="heading"><%= word_count %> <%= "word".pluralize(word_count) %> <%= "'#{ tag }'" if tag.present? %> in Total</h3>
      <% end %>
    </div>
    <div class="text-right">
      <%= search_form_for q, url: search_dictionary_path, class: "form-inline", local: true do |f| %>
        <%= f.label :name_or_meaning_or_start, "単語 / 意味", class: "control-label" %>&emsp;
        <%= f.search_field :name_or_meaning_start, class: "form-control" %>
        <%= f.submit "検索", class: "btn btn-default" %>
      <% end %>
    </div>
    <p>
      <span class="bg-warning">YELLOW</span>, 投稿単語で、辞書に登録されております。<br>
      <span class="bg-info">BLUE</span>, 投稿単語ですが、辞書からは外されております。
    </p>
    
    <div class="row margin-top-sm">
      <% words.each do |word| %>
        <div class="col-xs-6 col-sm-4 col-md-3">
          <%= render "layouts/card", word: word %>
        </div>
      <% end %>
    </div>
    <%= paginate words, window: 1 %>
  </div>
</div>
